decisions = {
	hide_toggles = {
		ai = no
		potential = {
			NOT = {
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			set_character_flag = hidden_toggles
		}
	}
	show_toggles = {
		ai = no
		potential = {
			has_character_flag = hidden_toggles
		}
		allow = {
			ai = no
		}
		effect = {
			clr_character_flag = hidden_toggles
		}
	}
    hide_pruning = { #Toggles pruning decisions, targetted and otherwise
		ai= no #Human only
		potential = { #Hides if module disabled in game rules, so they are still master toggles, for multiplayer regards
			has_game_rule = {
				name = tree_pruning
				value = enabled
			}
			NOR = { #Doesn't have the flag already, this makes it a toggle rather than a clicky thing
				has_character_flag = hidden_pruning #Flags are completely custom, character based means it's multiplayer compatible too, does mean you need to retoggle when your character dies though, not too big an issue though.
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			set_character_flag = hidden_pruning #Sets flag
		}
	}
	show_pruning = {
		ai= no
		potential = {
			has_game_rule = {
				name = tree_pruning
				value = enabled
			}
			has_character_flag = hidden_pruning #Makes this decision only appear when the toggle off flag is enabled
			NOT = {
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			clr_character_flag = hidden_pruning #Gets rid of flag, no need for shown_pruning flag because you can check for the absence of hidden, less flags, the better.
		}
	}
    hide_mp_commands = {
		ai= no
		potential = {
			has_game_rule = {
				name = multiplayer_commands
				value = enabled
			}
			NOR = {
				has_character_flag = hidden_mp_commands
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			set_character_flag = hidden_mp_commands
		}
	}
	show_mp_commands = {
		ai= no
		potential = {
			has_game_rule = {
				name = multiplayer_commands
				value = enabled
			}
			has_character_flag = hidden_mp_commands
			NOT = {
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			clr_character_flag = hidden_mp_commands
		}
	}
    hide_renaming = {
		ai= no
		potential = {
			has_game_rule = {
				name = rename_family
				value = enabled
			}
			NOR = {
				has_character_flag = hidden_renaming
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			set_character_flag = hidden_renaming
		}
	}
	show_renaming = {
		ai= no
		potential = {
			has_game_rule = {
				name = rename_family
				value = enabled
			}
			has_character_flag = hidden_renaming
			NOT = {
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			clr_character_flag = hidden_renaming
		}
	}
    hide_yam = {
		ai= no
		potential = {
			has_game_rule = {
				name = artifact_mod
				value = enabled
			}
			NOR = {
				has_character_flag = hidden_yam
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			set_character_flag = hidden_yam
		}
	}
	show_yam = {
		ai= no
		potential = {
			has_game_rule = {
				name = artifact_mod
				value = enabled
			}
			has_character_flag = hidden_yam
			NOT = {
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			clr_character_flag = hidden_yam
		}
	}
    hide_nap = {
		ai= no
		potential = {
			has_game_rule = {
				name = non_aggression_pact_decision
				value = enabled
			}
			NOR = {
				has_character_flag = hidden_nap
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			set_character_flag = hidden_nap
		}
	}
	show_nap = {
		ai= no
		potential = {
			has_game_rule = {
				name = non_aggression_pact_decision
				value = enabled
			}
			has_character_flag = hidden_nap
			NOT = {
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			clr_character_flag = hidden_nap
		}
	}
	hide_ystt = {
		ai= no
		potential = {
			has_game_rule = {
				name = sex_change
				value = enabled
			}
			NOR = {
				has_character_flag = hidden_ystt
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			set_character_flag = hidden_ystt #Sets flag
		}
	}
	show_ystt = {
		ai= no
		potential = {
			has_game_rule = {
				name = sex_change
				value = enabled
			}
			has_character_flag = hidden_ystt #Makes this decision only appear when the toggle off flag is enabled
			NOT = {
				has_character_flag = hidden_toggles
			}
		}
		allow = {
			ai = no
		}
		effect = {
			clr_character_flag = hidden_ystt #Gets rid of flag, no need for shown_pruning flag because you can check for the absence of hidden, less flags, the better.
		}
	}
}